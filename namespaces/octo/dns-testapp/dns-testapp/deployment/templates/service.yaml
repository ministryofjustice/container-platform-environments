apiVersion: v1
kind: Service
metadata:
  name: dns-testapp
  labels:
    app: dns-testapp
spec:
  selector:
    app: dns-testapp
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
