# Routing metadata (the `namespace` key is consumed by the workload ApplicationSet)
namespace: test-app-prod

# Helm value overrides for prod
replicaCount: 2
host: test-app.octo-live.container-platform.service.justice.gov.uk
gateway:
  name: default
  namespace: envoy-gateway-system
