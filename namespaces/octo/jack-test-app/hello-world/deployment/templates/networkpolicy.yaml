apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: helloworld-allow-gateway
  labels:
    app: helloworld
spec:
  podSelector:
    matchLabels:
      app: helloworld
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: {{ .Values.gateway.namespace | quote }}
      ports:
        - protocol: TCP
          port: {{ .Values.service.targetPort }}
