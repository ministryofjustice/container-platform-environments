apiVersion: v1
kind: Service
metadata:
  name: dan-testapp
  labels:
    app: dan-testapp
spec:
  selector:
    app: dan-testapp
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP