apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: dan-testapp-allow-gateway
  labels:
    app: dan-testapp
spec:
  podSelector:
    matchLabels:
      app: dan-testapp
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: envoy-gateway-system
      ports:
        - protocol: TCP
          port: {{ .Values.service.targetPort }}