apiVersion: v1
kind: Service
metadata:
  name: william-app
  labels:
    app: william-app
spec:
  selector:
    app: william-app
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
